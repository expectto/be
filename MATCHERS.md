# be — matcher catalog

<!-- Code generated by internal/docgen (via generate-docs.sh). DO NOT EDIT. -->

Every assertion helper and matcher across all `be` packages, grouped by
intent. The **Instead of** column lists the raw idiom or anti-pattern the
matcher supersedes — prefer the matcher: its failure message shows the
values involved, `be.True(<expr>)` only reports "expected true".

Matcher arguments may be raw values **or other matchers** (Be/Gomega/Gomock),
so matchers compose: `be.HaveLength(be.Gte(3))`, `be.ContainElement(be.HaveField("Name", "X"))`.

## Assertions & shortcuts

| Matcher | What it does | Instead of |
|---|---|---|
| `be.AssertThat(t TestingT, actual, matcher any, msgAndArgs ...any)` | AssertThat is the flat, testify-style spelling of Expect(t, actual).To(matcher): a soft assertion that reports via Errorf and lets the test continue. |  |
| `be.RequireThat(t TestingT, actual, matcher any, msgAndArgs ...any)` | RequireThat is the flat, testify-style spelling of Require(t, actual).To(matcher): a hard assertion that stops the test on the first failure via Fatalf. |  |
| `be.NoError(t TestingT, err error, msgAndArgs ...any)` | NoError fails the test immediately (Fatalf) if err is non-nil. | `if err != nil { t.Fatal(err) }` |
| `be.Error(t TestingT, err error, msgAndArgs ...any)` | Error fails the test immediately (Fatalf) if err is nil. |  |
| `be.ErrorIs(t TestingT, err, target error, msgAndArgs ...any)` | ErrorIs fails the test immediately (Fatalf) unless errors.Is(err, target). |  |

## Async

| Matcher | What it does | Instead of |
|---|---|---|
| `be.Eventually(t TestingT, actual any, matcher any, opts ...EventuallyOption)` | Eventually polls actual until it satisfies the matcher, failing the test (softly, via Errorf) if it never does within the timeout. |  |
| `be.Consistently(t TestingT, actual any, matcher any, opts ...EventuallyOption)` | Consistently polls actual and requires it to satisfy the matcher on EVERY poll for the whole duration (default 100ms, set via WithTimeout). |  |

## Equality & identity

| Matcher | What it does | Instead of |
|---|---|---|
| `be.Eq(expected any)` | Eq succeeds if actual equals expected by VALUE (deep equality, like gomega.Equal) | `be.True(x == y)` |
| `be.Ne(expected any)` | Ne succeeds if actual is NOT equal to expected (the negation of Eq) | `be.True(x != y)`, `be.Not(be.Eq(y))` |
| `be.Identical(expected any)` | Identical succeeds if actual is identical to expected using Go's == operator (pointer identity for pointers). |  |
| `be.NotIdentical(expected any)` | NotIdentical succeeds if actual is NOT identical to expected (the negation of Identical). |  |
| `be.Zero()` | Zero succeeds if actual is the zero value for its type: 0, "", nil, false, a zero struct, etc. | `be.Eq(0)` for "unset" |
| `be.NonZero()` | NonZero succeeds if actual is NOT the zero value for its type | `be.Not(be.Eq(0))`, `be.Ne(0)` |

## Errors

| Matcher | What it does | Instead of |
|---|---|---|
| `be.Succeed()` | Succeed succeeds if actual is a nil error. |  |
| `be.HaveOccurred()` | HaveOccurred succeeds if actual is a non-nil error. |  |
| `be.MatchError(expected any)` | MatchError succeeds if actual is an error matching expected. | `be.True(errors.Is(err, X))` |
| `be.MatchErrorAs[T error]()` | MatchErrorAs succeeds if actual is an error that matches type T via errors.As — the matcher spelling of `var target T; errors.As(err, &target)` | `var v E` + `be.True(errors.As(err, &v))` |

## Booleans, nil & panics

| Matcher | What it does | Instead of |
|---|---|---|
| `be.True()` | True succeeds if actual is the boolean true. |  |
| `be.False()` | False succeeds if actual is the boolean false. |  |
| `be.Nil()` | Nil succeeds if actual is nil. |  |
| `be.NotNil()` | NotNil succeeds if actual is not nil | `be.Not(be.Nil())` |
| `be.Panic()` | Panic succeeds if actual is a func() that panics when invoked. |  |
| `be.NotPanic()` | NotPanic succeeds if actual is a func() that does not panic when invoked. |  |

## Collections & length

| Matcher | What it does | Instead of |
|---|---|---|
| `be.ContainElement(element any)` | ContainElement succeeds if actual (a slice, array or map) contains an element that matches the given value or matcher | `be.True(slices.Contains(xs, v))` |
| `be.ContainElements(elements ...any)` | ContainElements succeeds if actual contains all of the given elements (each may be a value or a matcher), in any order. |  |
| `be.HaveKey(key any)` | HaveKey succeeds if actual (a map) has a key matching the given value or matcher | `_, ok := m[k]` + `be.True(ok)` |
| `be.HaveKeyWithValue(key, value any)` | HaveKeyWithValue succeeds if actual (a map) has the given key with a matching value. |  |
| `be.Empty()` | Empty succeeds if actual is empty: a zero-length string, slice, array, map or channel (like gomega.BeEmpty) | `be.HaveLength(0)`, `be.True(len(xs) == 0)` |
| `be.NotEmpty()` | NotEmpty succeeds if actual is not empty | `be.Not(be.HaveLength(0))`, `be.True(len(xs) > 0)` |
| `be.HaveLength(args ...any)` | HaveLength succeeds if the actual value (string, slice, array, map or channel) has a length matching the provided condition — either an exact count, or one or more matchers applied to the length (unlike gomega.HaveLen, which only takes a count) | `be.True(len(xs) >= n)` |
| `be.Dive(matcher any)` | Dive applies the given matcher to each (every) element of a slice or array, or to each value of a map. |  |
| `be.DiveAny(matcher any)` | DiveAny applies the given matcher to each element and succeeds in case if it succeeds at least at one item |  |
| `be.DiveFirst(matcher any)` | DiveFirst applies the given matcher to the first element of the given slice |  |
| `be.DiveNth(n int, matcher any)` | DiveNth applies the given matcher to the nth element of the given slice |  |

## Structs

| Matcher | What it does | Instead of |
|---|---|---|
| `be.HaveField(field string, value any)` | HaveField succeeds if actual is a struct (or pointer to one) whose field — or nil-safe method chain — matches the given value or matcher. | `be.Eq(x.Field)` on a projected value |
| `be.HaveFields(fields map[string]any)` | HaveFields succeeds if actual matches HaveField for every entry of the given map (logical AND). |  |
| `be_struct.HavingField[StructT any](fieldName string, expectedValue ...any)` | HavingField succeeds if the actual value is a struct of exactly type StructT and it has a field with the given name. |  |

## Numbers

| Matcher | What it does | Instead of |
|---|---|---|
| `be.Gt(...)` | Gt is an alias for be_math.Gt: succeeds if actual is numerically > arg. | `be.True(x > n)` |
| `be.Gte(...)` | Gte is an alias for be_math.Gte: succeeds if actual is numerically >= arg. | `be.True(x >= n)` |
| `be.Lt(...)` | Lt is an alias for be_math.Lt: succeeds if actual is numerically < arg. | `be.True(x < n)` |
| `be.Lte(...)` | Lte is an alias for be_math.Lte: succeeds if actual is numerically <= arg. | `be.True(x <= n)` |
| `be.GreaterThan(...)` | GreaterThan is an alias for be_math.GreaterThan (long spelling of Gt). |  |
| `be.GreaterThanEqual(...)` | GreaterThanEqual is an alias for be_math.GreaterThanEqual (long spelling of Gte). |  |
| `be.LessThan(...)` | LessThan is an alias for be_math.LessThan (long spelling of Lt). |  |
| `be.LessThanEqual(...)` | LessThanEqual is an alias for be_math.LessThanEqual (long spelling of Lte). |  |
| `be.InRange(...)` | InRange is an alias for be_math.InRange: succeeds if actual is within [from, until] with configurable inclusivity. |  |
| `be.Positive(...)` | Positive is an alias for be_math.Positive: succeeds if actual is > 0. |  |
| `be.Negative(...)` | Negative is an alias for be_math.Negative: succeeds if actual is < 0. |  |
| `be_math.Approx(compareTo, threshold any)` | Approx succeeds if actual is numerically approximately equal to the passed-in value within the specified threshold. |  |
| `be_math.ApproxZero()` | ApproxZero succeeds if actual is numerically approximately equal to zero Any type of int/float will work for comparison. |  |
| `be_math.DivisibleBy(divisor any)` | DivisibleBy succeeds if actual is numerically divisible by the passed-in value. |  |
| `be_math.Even()` | Even succeeds if actual is an even numeric value. |  |
| `be_math.GreaterThan(arg any)` | GreaterThan succeeds if actual is numerically greater than the passed-in value. |  |
| `be_math.GreaterThanEqual(arg any)` | GreaterThanEqual succeeds if actual is numerically greater than or equal to the passed-in value. |  |
| `be_math.Gt(arg any)` | Gt is an alias for GreaterThan, succeeding if actual is numerically greater than the passed-in value. |  |
| `be_math.Gte(arg any)` | Gte is an alias for GreaterThanEqual, succeeding if actual is numerically greater than or equal to the passed-in value. |  |
| `be_math.InRange(from any, fromInclusive bool, until any, untilInclusive bool)` | InRange succeeds if actual is numerically within the specified range. |  |
| `be_math.Integral()` | Integral succeeds if actual is an integral float, meaning it has zero decimal places. |  |
| `be_math.LessThan(arg any)` | LessThan succeeds if actual is numerically less than the passed-in value. |  |
| `be_math.LessThanEqual(arg any)` | LessThanEqual succeeds if actual is numerically less than or equal to the passed-in value. |  |
| `be_math.Lt(arg any)` | Lt is an alias for LessThan, succeeding if actual is numerically less than the passed-in value. |  |
| `be_math.Lte(arg any)` | Lte is an alias for LessThanEqual, succeeding if actual is numerically less than or equal to the passed-in value. |  |
| `be_math.Negative()` | Negative succeeds if actual is a negative numeric value. |  |
| `be_math.Odd()` | Odd succeeds if actual is an odd numeric value. |  |
| `be_math.Positive()` | Positive succeeds if actual is a positive numeric value. |  |
| `be_math.Zero()` | Zero succeeds if actual is numerically equal to zero. |  |

## Strings

| Matcher | What it does | Instead of |
|---|---|---|
| `be.ContainSubstring(substr string)` | ContainSubstring succeeds if actual is a string containing the given substring. | `be.True(strings.Contains(s, q))` |
| `be.StringAsTemplate(...)` | StringAsTemplate is an alias for be_string.MatchTemplate matcher |  |
| `be_string.ContainingCharacters(characters string)` | ContainingCharacters succeeds if actual is a string containing all characters from a given set |  |
| `be_string.ContainingOnlyCharacters(characters string)` | ContainingOnlyCharacters succeeds if actual is a string containing only characters from a given set |  |
| `be_string.ContainingSubstring(substr string)` | ContainingSubstring succeeds if actual is a string containing only characters from a given set |  |
| `be_string.EmptyString()` | EmptyString succeeds if actual is an empty string. |  |
| `be_string.Float()` | Float succeeds if actual is a string representing a valid floating-point number. |  |
| `be_string.HavingPrefix(prefix string)` | HavingPrefix succeeds if actual is a string starting with the given prefix — the matcher spelling of strings.HasPrefix | `be.True(strings.HasPrefix(s, p))` |
| `be_string.HavingSuffix(suffix string)` | HavingSuffix succeeds if actual is a string ending with the given suffix — the matcher spelling of strings.HasSuffix | `be.True(strings.HasSuffix(s, x))` |
| `be_string.LowerCaseOnly()` | LowerCaseOnly succeeds if actual is a string containing only lowercase characters. |  |
| `be_string.MatchTemplate(template string, values ...*psi_matchers.Value)` | MatchTemplate succeeds if actual matches given template pattern. |  |
| `be_string.MatchWildcard(pattern string)` | MatchWildcard succeeds if actual matches given wildcard pattern. |  |
| `be_string.NonEmptyString()` | NonEmptyString succeeds if actual is not an empty string. |  |
| `be_string.Only(option StringOption)` | Only succeeds if actual is a string containing only characters described by given options Only() defaults to empty string matching Only(Alpha\|Numeric) succeeds if string contains only from alphabetic and numeric characters Available options are: Alpha, Numeric, Whitespace, Dots, Punctuation, SpecialCharacters TODO: special-characters are not supported yet |  |
| `be_string.Titled(languageArg ...language.Tag)` | Titled succeeds if actual is a string with the first letter of each word capitalized. |  |
| `be_string.UpperCaseOnly()` | UpperCaseOnly succeeds if actual is a string containing only uppercase characters. |  |
| `be_string.ValidEmail()` | ValidEmail succeeds if actual is a valid email. |  |

## Time

| Matcher | What it does | Instead of |
|---|---|---|
| `be_time.Approx(compareTo time.Time, threshold time.Duration)` | Approx succeeds if actual time is approximately equal to the specified time `compareTo` within the given time duration threshold. | `be.True(d < time.Second)` on a time diff |
| `be_time.Day(v int)` |  |  |
| `be_time.EarlierThan(compareTo time.Time)` | EarlierThan succeeds if actual time is earlier than the specified time `compareTo`. |  |
| `be_time.EarlierThanEqual(compareTo time.Time)` | EarlierThanEqual succeeds if actual time is earlier than or equal to the specified time `compareTo`. |  |
| `be_time.Eq(compareTo time.Time)` | Eq succeeds if actual time is equal to the specified time `compareTo` with the precision of one nanosecond. |  |
| `be_time.IsDST(compareTo time.Time)` | IsDST checks if actual time is DST |  |
| `be_time.LaterThan(compareTo time.Time)` | LaterThan succeeds if actual time is later than the specified time `compareTo`. |  |
| `be_time.LaterThanEqual(compareTo time.Time)` | LaterThanEqual succeeds if actual time is later than or equal to the specified time `compareTo`. |  |
| `be_time.Month(monthCompareTo time.Month)` |  |  |
| `be_time.SameDay(compareTo time.Time)` | SameDay checks if the .Day() component of the actual time matches the .Day() component of the specified time `compareTo`. |  |
| `be_time.SameExactDay(compareTo time.Time)` | SameExactDay succeeds if the actual time falls within the same day as the specified time `compareTo`. |  |
| `be_time.SameExactHour(compareTo time.Time)` | SameExactHour succeeds if the actual time falls within the same hour as the specified time `compareTo`. |  |
| `be_time.SameExactMilli(compareTo time.Time)` | SameExactMilli succeeds if the actual time falls within the same millisecond as the specified time `compareTo`. |  |
| `be_time.SameExactMinute(compareTo time.Time)` | SameExactMinute succeeds if the actual time falls within the same minute as the specified time `compareTo`. |  |
| `be_time.SameExactMonth(compareTo time.Time)` | SameExactMonth succeeds if the actual time falls within the same month as the specified time `compareTo`. |  |
| `be_time.SameExactSecond(compareTo time.Time)` | SameExactSecond succeeds if the actual time falls within the same second as the specified time `compareTo`. | `be.True(t1.Equal(t2))` |
| `be_time.SameExactWeek(compareTo time.Time)` | SameExactWeek succeeds if the actual time falls within the same ISO week as the specified time `compareTo`. |  |
| `be_time.SameExactWeekday(compareTo time.Time)` | SameExactWeekday succeeds if the weekday component of the actual time is equal to the weekday component of the specified time `compareTo`. |  |
| `be_time.SameHour(compareTo time.Time)` | SameHour checks if the .Hour() component of the actual time matches the .Hour() component of the specified time `compareTo`. |  |
| `be_time.SameMinute(compareTo time.Time)` | SameMinute checks if the .Minute() component of the actual time matches the .Minute() component of the specified time `compareTo`. |  |
| `be_time.SameMonth(compareTo time.Time)` | SameMonth checks if the .Month() component of the actual time matches the .Month() component of the specified time `compareTo`. |  |
| `be_time.SameOffset(compareTo time.Time)` | SameOffset checks if actual time is the same timezone offset as specified time `compareTo` Note: times can have different timezone names, but same offset, e.g. |  |
| `be_time.SameSecond(compareTo time.Time)` | SameSecond checks if the .Second() component of the actual time matches the .Second() component of the specified time `compareTo`. |  |
| `be_time.SameTimezone(compareTo time.Time)` | SameTimezone checks if actual time is the same timezone as specified time `compareTo` |  |
| `be_time.SameWeek(compareTo time.Time)` | SameWeek succeeds if the ISO week of the actual time are equal to the ISO week and year components of the specified time `compareTo`. |  |
| `be_time.SameYear(compareTo time.Time)` | SameYear succeeds if the year component of the actual time is equal to the year component of the specified time `compareTo`. |  |
| `be_time.SameYearDay(compareTo time.Time)` | SameYearDay checks if the .YearDay() component of the actual time matches the .YearDay() component of the specified time `compareTo`. |  |
| `be_time.Unix(v int64)` |  |  |
| `be_time.Weekday(v time.Weekday)` |  |  |
| `be_time.Year(v int)` |  |  |
| `be_time.YearDay(v int)` |  |  |

## Composition & control

| Matcher | What it does | Instead of |
|---|---|---|
| `be.All(ms ...any)` | All is like gomega.And() |  |
| `be.Any(ms ...any)` | Any is like gomega.Or() |  |
| `be.Not(expected any)` | Not is like gomega.Not() |  |
| `be.Always()` | Always does always match |  |
| `be.Never(err error)` | Never does never succeed (does always fail) |  |
| `be.Via(transform any, matcher any)` | Via applies the transform function to the actual value and matches the result against the given matcher. |  |

## Types & kinds

| Matcher | What it does | Instead of |
|---|---|---|
| `be_reflected.AsBytes()` | AsBytes succeeds if actual is assignable to a slice of bytes ([]byte). |  |
| `be_reflected.AsChan()` | AsChan succeeds if actual is of kind reflect.Chan. |  |
| `be_reflected.AsFinalPointer()` | AsFinalPointer succeeds if the actual value is a final pointer, meaning it's a pointer to a non-pointer type. |  |
| `be_reflected.AsFloat()` | AsFloat succeeds if actual is a numeric value that represents a floating-point value. |  |
| `be_reflected.AsFloatString()` | AsFloatString succeeds if actual is a string that can be parsed into a valid floating-point value. |  |
| `be_reflected.AsFunc()` | AsFunc succeeds if actual is of kind reflect.Func. |  |
| `be_reflected.AsInteger()` | AsInteger succeeds if actual is a numeric value that represents an integer (from reflect.Int up to reflect.Uint64). |  |
| `be_reflected.AsIntegerString()` | AsIntegerString succeeds if actual is a string that can be parsed into a valid integer value. |  |
| `be_reflected.AsKind(args ...any)` | AsKind succeeds if actual is assignable to any of the specified kinds or matches the provided matchers. |  |
| `be_reflected.AsMap()` | AsMap succeeds if actual is of kind reflect.Map. |  |
| `be_reflected.AsNumber()` | AsNumber succeeds if actual is a numeric value, supporting various integer kinds: reflect.Int, ... |  |
| `be_reflected.AsNumeric()` | AsNumeric succeeds if actual is any numeric value — an integer (signed or unsigned) or a float. |  |
| `be_reflected.AsNumericString()` | AsNumericString succeeds if actual is a string that can be parsed into a valid numeric value. |  |
| `be_reflected.AsObject()` | AsObject is more specific than AsMap. |  |
| `be_reflected.AsObjects()` |  |  |
| `be_reflected.AsPointer()` | AsPointer succeeds if the actual value is a pointer. |  |
| `be_reflected.AsPointerToMap()` | AsPointerToMap succeeds if actual is a pointer to a map. |  |
| `be_reflected.AsPointerToObject()` | AsPointerToObject succeeds if actual is a pointer to a value that matches AsObject after applying dereference. |  |
| `be_reflected.AsPointerToSlice()` | AsPointerToSlice succeeds if actual is a pointer to a slice. |  |
| `be_reflected.AsPointerToStruct()` | AsPointerToStruct succeeds if actual is a pointer to a struct. |  |
| `be_reflected.AsReader()` | AsReader succeeds if actual implements the io.Reader interface. |  |
| `be_reflected.AsSlice()` | AsSlice succeeds if actual is of kind reflect.Slice. |  |
| `be_reflected.AsSliceOf[T any]()` | AsSliceOf succeeds if actual is of kind reflect.Slice and each element of the slice is assignable to the specified type T. |  |
| `be_reflected.AsString()` | AsString succeeds if actual is of kind reflect.String. |  |
| `be_reflected.AsStringer()` | AsStringer succeeds if actual implements the fmt.Stringer interface. |  |
| `be_reflected.AsStruct()` | AsStruct succeeds if actual is of kind reflect.Struct. |  |
| `be_reflected.AssignableTo[T any]()` | AssignableTo succeeds if actual is assignable to the specified type T. |  |
| `be_reflected.Implementing[T any]()` | Implementing succeeds if actual implements the specified interface type T. |  |

## HTTP, URL, JSON, JWT & context

| Matcher | What it does | Instead of |
|---|---|---|
| `be.HttpRequest(...)` | HttpRequest is an alias for be_http.Request matcher |  |
| `be.URL(...)` | URL is an alias for be_url.URL matcher |  |
| `be.JSON(...)` | JSON is an alias for be_json.JSON matcher |  |
| `be.JwtToken(...)` | JwtToken is an alias for be_jwt.Token matcher |  |
| `be.Ctx(...)` | Ctx is an alias for be_ctx.Ctx |  |
| `be_http.HavingBody(args ...any)` | HavingBody succeeds if the actual value is a *http.Request and its body matches the provided arguments. |  |
| `be_http.HavingCtx(args ...any)` | HavingCtx succeeds if the actual value is a *http.Request whose context (req.Context()) matches the provided arguments. |  |
| `be_http.HavingHeader(key string, args ...any)` | HavingHeader matches requests that have header with a given key. |  |
| `be_http.HavingHeaders(key string, args ...any)` | HavingHeaders matches requests that have header with a given key. |  |
| `be_http.HavingHost(args ...any)` | HavingHost succeeds if the actual value is a *http.Request and its Host matches the provided arguments. |  |
| `be_http.HavingMethod(args ...any)` | HavingMethod succeeds if the actual value is a *http.Request and its HTTP method matches the provided arguments. |  |
| `be_http.HavingProto(args ...any)` | HavingProto succeeds if the actual value is a *http.Request and its Proto matches the provided arguments. |  |
| `be_http.HavingURL(args ...any)` | HavingURL succeeds if the actual value is a *http.Request and its URL matches the provided arguments. |  |
| `be_http.Request(args ...any)` | Request matches an actual value to be a valid *http.Request corresponding to given inputs. |  |
| `be_http.GET(...)` | HavingMethod: Syntactic sugar |  |
| `be_http.HEAD(...)` | HavingMethod: Syntactic sugar |  |
| `be_http.POST(...)` | HavingMethod: Syntactic sugar |  |
| `be_http.PUT(...)` | HavingMethod: Syntactic sugar |  |
| `be_http.PATCH(...)` | HavingMethod: Syntactic sugar |  |
| `be_http.DELETE(...)` | HavingMethod: Syntactic sugar |  |
| `be_http.OPTIONS(...)` | HavingMethod: Syntactic sugar |  |
| `be_http.CONNECT(...)` | HavingMethod: Syntactic sugar |  |
| `be_http.TRACE(...)` | HavingMethod: Syntactic sugar |  |
| `be_url.HavingHost(args ...any)` | HavingHost succeeds if the actual value is a *url.URL and its Host matches the provided one (via direct value or matchers) |  |
| `be_url.HavingHostname(args ...any)` | HavingHostname succeeds if the actual value is a *url.URL and its Hostname matches the provided one (via direct value or matchers) |  |
| `be_url.HavingMultipleSearchParam(searchParamName string, args ...any)` | HavingMultipleSearchParam succeeds if the actual value is a *url.URL and its specified search parameter (all its values via slice) matches the provided arguments. |  |
| `be_url.HavingPassword(args ...any)` | HavingPassword succeeds if the actual value is a *url.URL and its Password matches the provided one. |  |
| `be_url.HavingPath(args ...any)` | HavingPath succeeds if the actual value is a *url.URL and its Path matches the given one. |  |
| `be_url.HavingPort(args ...any)` | HavingPort succeeds if the actual value is a *url.URL and its Port matches the provided one. |  |
| `be_url.HavingRawQuery(args ...any)` | HavingRawQuery succeeds if the actual value is a *url.URL and its RawQuery matches the given one. |  |
| `be_url.HavingScheme(args ...any)` | HavingScheme succeeds if the actual value is a *url.URL and its Scheme matches the provided one (via direct value or matchers) |  |
| `be_url.HavingSearchParam(searchParamName string, args ...any)` | HavingSearchParam succeeds if the actual value is a *url.URL and its specified search parameter matches the provided arguments. |  |
| `be_url.HavingUserinfo(args ...any)` | HavingUserinfo succeeds if the actual value is a *url.URL and its User.String() matches the provided one. |  |
| `be_url.HavingUsername(args ...any)` | HavingUsername succeeds if the actual value is a *url.URL and its Username matches the provided one. |  |
| `be_url.NotHavingPort(args ...any)` | NotHavingPort succeeds if the actual value is a *url.URL and its Port does not match the given one. |  |
| `be_url.NotHavingScheme(args ...any)` | NotHavingScheme succeeds if the actual value is a *url.URL and its Scheme negatively matches given value Example: `Expect(u).To(NotHavingScheme())` matches url without a scheme |  |
| `be_url.NotHavingSearchParam(searchParamName string)` | NotHavingSearchParam succeeds if the URL's query does NOT contain the given parameter at all (distinct from present-but-empty). |  |
| `be_url.URL(args ...any)` | URL matches actual value to be a valid URL corresponding to given inputs Possible inputs: 1. |  |
| `be_url.Values(args ...any)` | Values matches a url.Values (e.g. |  |
| `be_url.WithHttp()` | WithHttp succeeds if the actual value is a *url.URL and its scheme is "http". |  |
| `be_url.WithHttps()` | WithHttps succeeds if the actual value is a *url.URL and its scheme is "https". |  |
| `be_url.TransformSchemelessUrlFromString(...)` | TransformSchemelessUrlFromString returns string->*url.Url transform It allows string to be a scheme-less url |  |
| `be_url.TransformUrlFromString(...)` | TransformUrlFromString returns string->*url.Url transform |  |
| `be_json.HaveKeyValue(key any, args ...any)` | HaveKeyValue is a facade to gomega.HaveKey & gomega.HaveKeyWithValue |  |
| `be_json.Matcher(args ...any)` | Matcher is a JSON matcher. |  |
| `be_jwt.HavingClaim(key string, args ...any)` | HavingClaim succeeds if the actual value is a JWT token and its claim matches the provided value or matchers. |  |
| `be_jwt.HavingClaims(args ...any)` | HavingClaims succeeds if the actual value is a JWT token and its claims match the provided value or matchers. |  |
| `be_jwt.HavingMethodAlg(args ...any)` | HavingMethodAlg succeeds if the actual value is a JWT token and its method algorithm match the provided value or matchers. |  |
| `be_jwt.SignedVia(secret string)` | SignedVia succeeds if the actual value is a valid and signed JWT token, verified using the specified secret key. |  |
| `be_jwt.Token(args ...any)` | Token matches the actual value to be a valid *jwt.Token corresponding to given inputs. |  |
| `be_jwt.Valid()` | Valid succeeds if the actual value is a JWT token and it's valid |  |
| `be_jwt.TransformJwtFromString(...)` | TransformJwtFromString is a transform function (string->*jwt.Token) without a secret. |  |
| `be_jwt.TransformSignedJwtFromString(...)` | TransformSignedJwtFromString returns a transform function (string->*jwt.Token) for a given secret. |  |
| `be_ctx.Ctx(args ...any)` | Ctx succeeds if the actual value is a context.Context. |  |
| `be_ctx.CtxWithDeadline(deadline any)` | CtxWithDeadline succeeds if the actual value is a context.Context and its deadline matches the provided deadline. |  |
| `be_ctx.CtxWithError(err any)` | CtxWithError succeeds if the actual value is a context.Context and its error matches the provided error value. |  |
| `be_ctx.CtxWithValue(key any, vs ...any)` | CtxWithValue succeeds if the actual value is a context.Context and contains a key-value pair where the key matches the provided key and the value matches the provided arguments using any other matchers. |  |

